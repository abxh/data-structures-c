/*
    Non-mutating operation types / properties:
        .prev_ptr
        .next_ptr
        list_node_entry()
        is_first()
        is_last()
        is_head()

    Mutating operation types:
        init()
        add_after()
        add_before()
        remove()
        replace()
    
    Usage:
        as a stack: using add_after()
        as a queue: using add_before()
*/


int main() {

}
